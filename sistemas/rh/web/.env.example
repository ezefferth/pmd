# RH rh-web — variáveis de ambiente (exemplo)

# URL base da api do RH
RH_API_URL="http://localhost:3005/api/v1"

# Município ativo (seleciona o tema em marca/) — multi-município
NEXT_PUBLIC_MUNICIPIO="dourados"
