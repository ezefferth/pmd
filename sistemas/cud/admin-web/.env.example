# CUD admin-web — variáveis de ambiente (exemplo)

# URL base da auth-api do CUD
AUTH_API_URL="http://localhost:3001/api/v1"

# Município ativo (seleciona o tema em marca/) — multi-município
NEXT_PUBLIC_MUNICIPIO="dourados"
